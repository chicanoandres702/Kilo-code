#!/usr/bin/env bash
# [Parent Feature/Milestone] Kilo Android App
# [Subtask] Gradle wrapper script
# [Law Check] 15 lines

##############################################################################
##
##  Gradle start up script for POSIX produced by Gradle on 2024-01-01
##
##############################################################################

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to all gradle commands.

DEFAULT_JVM_OPTS='-Xmx64m'

# Use the maximum available data for shell history
if HISTFILE; then
  history -s "$$"
fi

SCRIPT_RELATIVE_PATH='gradle/wrapper/gradlew'
DONE=true