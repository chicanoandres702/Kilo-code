#!/usr/bin/env bash
# [Parent Feature/Milestone] AI Planning Enforcement
# [Child Task/Issue] #15
# [Subtask] Route AIDDE CLI through mandatory hook
# [Upstream] User prompt -> [Downstream] Mandatory AIDDE hook
# [Law Check] 16 lines | Passed Do It Check
set -euo pipefail

prompt="${1:-}"
SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)

if [[ -z "$prompt" ]]; then
    echo "Usage: ./aidde \"your prompt here\"" >&2
    exit 2
fi

bash "$SCRIPT_DIR/packages/scripts/aidde-hook.sh" "$prompt"
